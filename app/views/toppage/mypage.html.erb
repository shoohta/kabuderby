<h1>ポートフォリオ</h1>
<h1>ユーザー名:<%= current_user.name %></h1>

<h1>購買余力:<%=  current_user.cash %>円</h1>
<h1>株の時価評価額合計:<%= current_user.total_value %>円</h1>
<h1>資産総額:<%=  current_user.cash + current_user.total_value %>円</h1>
<h1>通算損益:<%=  current_user.cash + current_user.total_value - 10000000 %>円</h1>
<br>
<h1>持ち株一覧</h1>
<table border="1">
  <tr>
    <th>会社名</th>
    <th>保有数量</th>
    <th>平均取得価額</th>
    <th>現在値</th>
    <th>時価評価額</th>
    <th>評価損益</th>
  </tr>
  <% @portfolios.each do |portfolio| %>
  <tr>
    <td><%= portfolio.company.name %></td>
    <td><%= portfolio.quantity %></td>
    <td><%= portfolio.average %></td>
    <td><%= portfolio.company.value %></td>
    <th><%= portfolio.company.value * portfolio.quantity %></th>
    <th><%= portfolio.company.value * portfolio.quantity - portfolio.average * portfolio.quantity  %>(<%= number_to_percentage(portfolio.company.value.quo(portfolio.average) * 100 - 100) %>)</th>
  </tr>
  <% end %> 
</table>

<h1><%= link_to 'トップページへ', '/toppage/index' %></h1>